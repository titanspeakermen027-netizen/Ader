// Cloudflare Pages runtime configuration.
// Ader's current backend public URL is used by default; change it only if the backend domain changes.
window.ADER_CONFIG = {
  API_BASE: "http://nova.hattena.com:25979",
  BRAND: "Ader",
  DEFAULT_LANGUAGE: "ar",
  DEFAULT_THEME: "discord"
};
